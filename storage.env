# Host storage layout for every stack. Tracked in git: these are host paths,
# not secrets. Not named `.env` because .gitignore excludes that everywhere.
#
# Applied automatically on deploy — src/ci-cd/watchtower/git-sync.sh passes this
# file to every stack as an extra --env-file, ahead of the stack's own .env.
#
# Manual use — the wrapper applies this file, a bare `docker compose` does not:
#   ./scripts/compose.sh src/qbittorrent up -d
#
# After changing a path here: move the data, run scripts/init-storage.sh, commit.

# 1 TB HDD, ext4, mounted by /etc/fstab. Holds all persistent service state.
DATA_ROOT=/mnt/homelab-data

# Jellyfin's video library ($MEDIA_ROOT/videos). Split onto another disk later
# by editing only this line.
MEDIA_ROOT=${DATA_ROOT}/media

# Navidrome's music library, mounted read-only. Points straight at the
# qBittorrent download directory because everything downloaded is music, so new
# torrents show up in Navidrome without being moved or copied.
# To serve a curated tree instead, set this to ${MEDIA_ROOT}/music and re-run
# scripts/init-storage.sh.
MUSIC_ROOT=${DATA_ROOT}/qbittorrent/downloads
