# Shared with the Nextcloud ONLYOFFICE app (Admin → ONLYOFFICE → Secret key).
# Generate once and keep stable across container recreates:
#   openssl rand -hex 32
JWT_SECRET=

# Must match ONLYOFFICE_DB_PASSWORD in src/mariadb/.env
ONLYOFFICE_DB_PASSWORD=
