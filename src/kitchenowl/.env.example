# Signs authentication tokens. Required — change before first start.
# Generate with:
#   openssl rand -hex 32
JWT_SECRET_KEY=

# Allow account creation until you have registered, then set false and
# `docker compose up -d` again.
OPEN_REGISTRATION=true
