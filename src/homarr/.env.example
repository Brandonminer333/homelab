# Required 64-character hex string used to encrypt secrets in Homarr's DB.
# Generate once and keep stable — changing it breaks existing encrypted data:
#   openssl rand -hex 32
SECRET_ENCRYPTION_KEY=
