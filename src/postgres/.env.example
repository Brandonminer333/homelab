# Superuser (admin / init only — apps do not use this)
POSTGRES_PASSWORD=

# Per-app roles (must match the same vars in each app's .env)
NEXTCLOUD_DB_PASSWORD=
FORGEJO_DB_PASSWORD=
ONLYOFFICE_DB_PASSWORD=
